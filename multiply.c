#include<stdio.h>

int main(){
      long long int num1, num2;
       scanf("%lld %lld", &num1, &num2);
       long long int multiply= num1 * num2;
       printf("%lld\n", multiply);
        return 0;
}